# Retrieval-Augmented Generation (RAG)

- \`RetrievalService\` uses \`SemanticSearchService\` to find relevant chunks.
- Builds multi-document context context chunks with inline citations.
- Prepares the AI Workspace for future advanced chat agents that can reason across entire workspaces.
